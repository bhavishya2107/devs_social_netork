const INITIAL_STATE = {
  isDeveloperAuthenticated: false,
  isAuthInProgress: true,
  developerDetails: null,
};

export default function developerReducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    case "value":
      
      break;
  
    default:
      return state
  }
}
